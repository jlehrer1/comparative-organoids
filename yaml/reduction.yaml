apiVersion: batch/v1
kind: Job
metadata:
  name: rna-seq-reduction
spec:
  backoffLimit: 0
  completions: 1
  template:
    metadata:
      labels:
        k8s-app: rna-seq-reduction
    spec:
      restartPolicy: Never
      containers:
      - name: rna-seq-reduction
        image: jmlehrer/cell-exploration:latest
        command: ["stdbuf", "-i0", "-o0", "-e0", "bash", "-c"]
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        args: ["python src/data/download_data.py && python src/models/reduction.py"]
        resources:
          limits:
            memory: 100Gi
            cpu: "24"
          requests:
            memory: 16Gi
            cpu: "4"
        volumeMounts:
        - name: ephemeral
          mountPath: "/data"
      volumes:
      - name: ephemeral
        emptyDir: {}