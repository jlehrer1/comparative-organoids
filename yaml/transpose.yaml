apiVersion: batch/v1
kind: Job
metadata:
  name: rna-seq-generate-transpose
spec:
  backoffLimit: 0
  completions: 1
  template:
    metadata:
      labels:
        k8s-app: rna-seq-generate-transpose
    spec:
      restartPolicy: Never
      containers:
      - name: rna-seq-generate-transpose
        image: jmlehrer/cell-exploration:latest
        command: ["stdbuf", "-i0", "-o0", "-e0", "bash", "-c"]
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        args: ["./unzip.sh && python src/data/generate_transposed_data.py"]
        resources:
          limits:
            memory: 100Gi
            cpu: "16"
          requests:
            memory: 72Gi
            cpu: "16"
        volumeMounts:
        - name: ephemeral
          mountPath: "/data"
      volumes:
      # Ephemeral storage
      - name: ephemeral
        emptyDir: {}